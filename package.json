{
  "name": "murmur0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "murmur0 — listens to the faint chatter Chrome extension wallets make on the wire, and scores it",
  "bin": {
    "audit": "bin/audit"
  },
  "scripts": {
    "audit": "tsx src/audit.ts",
    "fetch-ext": "tsx src/fetch-extension.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "puppeteer": "^24.0.0",
    "tldts": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
